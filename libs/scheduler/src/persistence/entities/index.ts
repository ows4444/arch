import { ScheduledJobEntity } from '../../domain/scheduled-job.entity';

export const SCHEDULER_TYPEORM_ENTITIES = [ScheduledJobEntity] as const;

export { ScheduledJobEntity } from '../../domain/scheduled-job.entity';
